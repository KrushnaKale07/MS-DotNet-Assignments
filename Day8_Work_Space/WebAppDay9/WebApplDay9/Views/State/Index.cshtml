@model IEnumerable<WebApplDay9.Models.State>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StateName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Contry.ContryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.StateName)
            </td>
            <td>
                @Html.DisplayFor(model => item.ContryId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-stateid="@item.StateId" class="btn-outline-warning>Edit</a> |
                    <a asp-action="Details" asp-route-stateid="@item.StateId" class="btn-outline-secondary">Details</a> |
                    <a asp-action="Delete" asp-route-stateid="@item.StateId" class="btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
